import { getSingletonHighlighter } from 'shiki';
import { visit } from 'unist-util-visit';
import { toString } from 'hast-util-to-string';
import rangeParser2 from 'parse-numeric-range';
import { unified } from 'unified';
import rehypeParse from 'rehype-parse';

// src/index.ts
function isJSONTheme(value) {
  return value ? Object.hasOwn(value, "tokenColors") : false;
}
function isElement(value) {
  return value ? value.type === "element" : false;
}
function isText(value) {
  return value ? value.type === "text" : false;
}
function isInlineCode(element, parent, bypass = false) {
  if (bypass) {
    return false;
  }
  return element.tagName === "code" && isElement(parent) && parent.tagName !== "pre" || element.tagName === "inlineCode";
}
function isBlockCode(element) {
  return element.tagName === "pre" && Array.isArray(element.children) && element.children.length === 1 && isElement(element.children[0]) && element.children[0].tagName === "code";
}
function getInlineCodeLang(meta, defaultFallbackLang) {
  const placeholder = "\0";
  let temp = meta.replace(/\\\\/g, placeholder);
  temp = temp.replace(/\\({:[a-zA-Z.-]+})$/, "$1");
  const lang = temp.match(/{:([a-zA-Z.-]+)}$/)?.[1];
  return lang?.replace(new RegExp(placeholder, "g"), "\\") || defaultFallbackLang;
}
function parseBlockMetaString(element, filter, defaultFallback) {
  let meta = filter(
    element.data?.meta ?? element.properties?.metastring ?? ""
  );
  const titleMatch = meta.match(/title="([^"]*)"/);
  const title = titleMatch?.[1] ?? null;
  meta = meta.replace(titleMatch?.[0] ?? "", "");
  const captionMatch = meta.match(/caption="([^"]*)"/);
  const caption = captionMatch?.[1] ?? null;
  meta = meta.replace(captionMatch?.[0] ?? "", "");
  let lang = defaultFallback;
  if (element.properties && Array.isArray(element.properties.className) && typeof element.properties.className[0] === "string" && element.properties.className[0].startsWith("language-")) {
    lang = element.properties.className[0].replace("language-", "");
  }
  return {
    title,
    caption,
    lang,
    meta
  };
}
function getThemeNames(theme) {
  if (isJSONTheme(theme)) {
    return [theme.name];
  }
  if (typeof theme === "string") {
    return [theme];
  }
  return Object.values(theme).map(
    (theme2) => typeof theme2 === "string" ? theme2 : theme2.name
  );
}
function replaceLineClass(element) {
  if (Array.isArray(element.properties?.className) && element.properties.className.includes("line")) {
    const className = element.properties.className.filter((c) => c !== "line");
    element.properties.className = className.length > 0 ? className : void 0;
    element.properties["data-line"] = "";
  }
}
var showLineNumbersRegex = /(?:^|\s)(showLineNumbers(?:\{(\d+)\})?)(?=\s|$)/g;
function getShowLineNumbers(meta, takenSpans) {
  for (const match of meta.matchAll(showLineNumbersRegex)) {
    const token = match[1];
    if (!token) continue;
    const start = (match.index ?? 0) + (match[0].length - token.length);
    const end = start + token.length;
    if (takenSpans.some(([from, to]) => start < to && end > from)) continue;
    return { startAt: match[2] ? Number(match[2]) : null };
  }
  return null;
}
function getLineId(lineNumber, meta) {
  const segments = meta.match(/\{[^}]+\}#[a-zA-Z0-9]+/g);
  if (!segments) return null;
  for (const segment of segments) {
    const [range, id] = segment.split("#");
    if (!(range && id)) continue;
    const match = range.match(/\{(.*?)\}/);
    const capture = match?.[1];
    if (capture && rangeParser2(capture).includes(lineNumber)) {
      return id;
    }
  }
  return null;
}

// src/chars/getElementsToHighlight.ts
function getContentLength(child) {
  if (isText(child)) {
    return child.value.length;
  }
  return isElement(child) ? toString(child).length : 0;
}
function isTextToken(child) {
  return isElement(child) && child.tagName === "span" && child.children.length === 1 && isText(child.children[0]);
}
function cloneToken(element, value) {
  const { id: _id, ...properties } = Object.fromEntries(
    Object.entries(element.properties).map(([key, property]) => [
      key,
      Array.isArray(property) ? [...property] : property
    ])
  );
  return {
    ...element,
    properties,
    children: [{ ...element.children[0], value }]
  };
}
function splitToken(parent, element, start, end) {
  const text = element.children[0];
  const value = text.value;
  const replacements = [];
  if (start > 0) {
    replacements.push(cloneToken(element, value.slice(0, start)));
  }
  text.value = value.slice(start, end);
  replacements.push(element);
  if (end < value.length) {
    replacements.push(cloneToken(element, value.slice(end)));
  }
  parent.children.splice(parent.children.indexOf(element), 1, ...replacements);
  return element;
}
function getContentRange(element, position) {
  let start = 0;
  for (let index = 0; index < element.children.length; index++) {
    const child = element.children[index];
    const end = start + getContentLength(child);
    if (start <= position && position < end) {
      return { child, index, start, end };
    }
    start = end;
  }
  return void 0;
}
function findTextRange(element, start, end) {
  let offset = 0;
  for (let childIndex = 0; childIndex < element.children.length; childIndex++) {
    const child = element.children[childIndex];
    const childStart = offset;
    offset += getContentLength(child);
    if (!(childStart <= start && end <= offset)) {
      continue;
    }
    if (isText(child)) {
      return {
        type: "range",
        parent: element,
        childIndex,
        start: start - childStart,
        end: end - childStart
      };
    }
    if (isElement(child)) {
      return findTextRange(child, start - childStart, end - childStart);
    }
  }
  return void 0;
}
function getSingleTarget(element, range, start, end) {
  if (isElement(range.child) && range.child.tagName === "span" && (isTextToken(range.child) || start === range.start && end === range.end)) {
    return {
      type: "elements",
      parent: element,
      elements: [
        isTextToken(range.child) ? splitToken(
          element,
          range.child,
          start - range.start,
          end - range.start
        ) : range.child
      ]
    };
  }
  if (isElement(range.child)) {
    return findTextRange(range.child, start - range.start, end - range.start);
  }
  if (isText(range.child)) {
    return {
      type: "range",
      parent: element,
      childIndex: range.index,
      start: start - range.start,
      end: end - range.start
    };
  }
  return void 0;
}
function getMultipleTarget(element, first, last, start, end) {
  const selectedChildren = element.children.slice(first.index, last.index + 1);
  const firstIsWhole = start === first.start;
  const lastIsWhole = end === last.end;
  if (!(selectedChildren.every(isElement) && (firstIsWhole || isTextToken(first.child)) && (lastIsWhole || isTextToken(last.child)))) {
    return void 0;
  }
  const firstMatch = firstIsWhole ? first.child : splitToken(
    element,
    first.child,
    start - first.start,
    first.end - first.start
  );
  const lastMatch = lastIsWhole ? last.child : splitToken(element, last.child, 0, end - last.start);
  const firstIndex = element.children.indexOf(firstMatch);
  const lastIndex = element.children.indexOf(lastMatch);
  return {
    type: "elements",
    parent: element,
    elements: element.children.slice(firstIndex, lastIndex + 1).filter(isElement)
  };
}
function getElementsToHighlight(element, start, end) {
  const first = getContentRange(element, start);
  const last = getContentRange(element, end - 1);
  if (!(first && last)) {
    return void 0;
  }
  return first.index === last.index ? getSingleTarget(element, first, start, end) : getMultipleTarget(element, first, last, start, end);
}

// src/chars/wrapHighlightedChars.ts
function applyHighlightProperties(element, chars, options) {
  element.properties["data-highlighted-chars"] = "";
  element.properties["data-chars-id"] = options.idsMap.get(chars);
}
function createHighlight(children, chars, options) {
  const element = {
    type: "element",
    tagName: "mark",
    properties: children.length === 1 && isText(children[0]) ? {} : { "data-highlighted-chars-mark": "" },
    children
  };
  applyHighlightProperties(element, chars, options);
  return element;
}
function wrapHighlightedChars(parentElement, elementsToWrap, chars, options) {
  if (elementsToWrap.length === 0) {
    return void 0;
  }
  const [singleElement] = elementsToWrap;
  const isSimpleSingleElement = elementsToWrap.length === 1 && singleElement.children.length === 1 && isText(singleElement.children[0]);
  if (isSimpleSingleElement) {
    const element2 = singleElement;
    applyHighlightProperties(element2, chars, options);
    element2.tagName = "mark";
    element2.children = [
      {
        type: "element",
        tagName: "span",
        properties: { style: element2.properties.style },
        children: element2.children
      }
    ];
    element2.properties.style = void 0;
    return element2;
  }
  const positions = elementsToWrap.map(
    (element2) => parentElement.children.indexOf(element2)
  );
  const isContiguous = positions.every(
    (position, index) => position !== -1 && (index === 0 || position === positions[index - 1] + 1)
  );
  if (!isContiguous) {
    return void 0;
  }
  const start = positions[0];
  const element = createHighlight(elementsToWrap, chars, options);
  parentElement.children.splice(start, elementsToWrap.length, element);
  return element;
}
function wrapHighlightedCharsInRange(parentElement, childIndex, start, end, chars, options) {
  const child = parentElement.children[childIndex];
  if (!isText(child)) {
    return void 0;
  }
  const highlight = createHighlight(
    [{ ...child, value: child.value.slice(start, end) }],
    chars,
    options
  );
  const replacements = [];
  if (start > 0) {
    replacements.push({ ...child, value: child.value.slice(0, start) });
  }
  replacements.push(highlight);
  if (end < child.value.length) {
    replacements.push({ ...child, value: child.value.slice(end) });
  }
  parentElement.children.splice(childIndex, 1, ...replacements);
  return highlight;
}

// src/chars/charsHighlighter.ts
function isAvailable(occupied, start, end) {
  for (let index = start; index < end; index++) {
    if (occupied[index] === 1) {
      return false;
    }
  }
  return true;
}
function findMatches(source, chars, charsIndex, occupied, options) {
  const selectedMatches = [];
  const currentRange = options.ranges[charsIndex] || [];
  const counterId = `${chars}-${charsIndex}`;
  let searchStart = 0;
  while (searchStart <= source.length - chars.length) {
    const matchStart = source.indexOf(chars, searchStart);
    if (matchStart === -1) {
      break;
    }
    const matchEnd = matchStart + chars.length;
    if (!isAvailable(occupied, matchStart, matchEnd)) {
      searchStart = matchStart + 1;
      continue;
    }
    const occurrence = (options.counterMap.get(counterId) || 0) + 1;
    options.counterMap.set(counterId, occurrence);
    occupied.fill(1, matchStart, matchEnd);
    if (currentRange.length === 0 || currentRange.includes(occurrence)) {
      selectedMatches.push({ chars, start: matchStart, end: matchEnd });
    }
    searchStart = matchEnd;
  }
  return selectedMatches;
}
function createMatchComponents(element, selectedMatches) {
  const ends = [];
  let offset = 0;
  for (const child of element.children) {
    offset += getContentLength(child);
    ends.push(offset);
  }
  const components = [];
  const matchesByPosition = [...selectedMatches].sort(
    (first, second) => first.start - second.start
  );
  let firstIndex = 0;
  let lastIndex = 0;
  for (const match of matchesByPosition) {
    while (firstIndex < element.children.length && ends[firstIndex] <= match.start) {
      firstIndex++;
    }
    lastIndex = Math.max(lastIndex, firstIndex);
    while (lastIndex < element.children.length && ends[lastIndex] < match.end) {
      lastIndex++;
    }
    const previous = components.at(-1);
    if (previous && firstIndex <= previous.lastIndex) {
      previous.lastIndex = Math.max(previous.lastIndex, lastIndex);
      previous.matches.push(match);
    } else {
      components.push({
        firstIndex,
        lastIndex,
        start: firstIndex === 0 ? 0 : ends[firstIndex - 1],
        matches: [match]
      });
    }
  }
  return components;
}
function applyMatches(element, selectedMatches, options) {
  const components = createMatchComponents(element, selectedMatches);
  for (let componentIndex = components.length - 1; componentIndex >= 0; componentIndex--) {
    const component = components[componentIndex];
    const container = {
      type: "element",
      tagName: "span",
      properties: {},
      children: element.children.slice(
        component.firstIndex,
        component.lastIndex + 1
      )
    };
    for (let index = component.matches.length - 1; index >= 0; index--) {
      const match = component.matches[index];
      const target = getElementsToHighlight(
        container,
        match.start - component.start,
        match.end - component.start
      );
      if (!target) {
        continue;
      }
      if (target.type === "elements") {
        match.element = wrapHighlightedChars(
          target.parent,
          target.elements,
          match.chars,
          options
        );
      } else {
        match.element = wrapHighlightedCharsInRange(
          target.parent,
          target.childIndex,
          target.start,
          target.end,
          match.chars,
          options
        );
      }
    }
    element.children.splice(
      component.firstIndex,
      component.lastIndex - component.firstIndex + 1,
      ...container.children
    );
  }
}
function charsHighlighter(element, charsList, options, onVisitHighlightedChars) {
  const source = toString(element);
  const occupied = new Uint8Array(source.length);
  const selectedMatches = [];
  charsList.forEach((chars, charsIndex) => {
    if (!chars) {
      return;
    }
    selectedMatches.push(
      ...findMatches(source, chars, charsIndex, occupied, options)
    );
  });
  applyMatches(element, selectedMatches, options);
  for (const match of selectedMatches) {
    if (match.element) {
      onVisitHighlightedChars?.(match.element, options.idsMap.get(match.chars));
    }
  }
}

// src/index.ts
function apply(element, {
  tree,
  lang,
  title,
  caption,
  inline = false,
  keepBackground = true,
  grid = true,
  lineNumbersMaxDigits = 1,
  theme,
  onVisitTitle,
  onVisitCaption
}) {
  element.tagName = inline ? "span" : "figure";
  element.properties["data-rehype-pretty-code-figure"] = "";
  const codeData = element.children[0]?.data;
  element.children = [tree].flatMap((tree2) => {
    const pre = tree2.children[0];
    const themeNames = getThemeNames(theme);
    const themeNamesString = themeNames.join(" ");
    if (!(isElement(pre) && pre.properties)) {
      return [];
    }
    const code = pre.children[0];
    if (Array.isArray(pre.properties.className) && pre.properties.className.includes("shiki")) {
      const className = pre.properties.className.filter(
        (c) => c !== "shiki" && c !== "shiki-themes" && (typeof c === "string" ? !themeNames.includes(c) : true)
      );
      pre.properties.className = className.length > 0 ? className : void 0;
    }
    if (!keepBackground) {
      pre.properties.style = void 0;
    }
    pre.properties["data-language"] = lang;
    pre.properties["data-theme"] = themeNamesString;
    if (!(isElement(code) && code.properties)) {
      return [];
    }
    code.properties["data-language"] = lang;
    code.properties["data-theme"] = themeNamesString;
    code.data = codeData;
    if (inline) {
      if (keepBackground) {
        code.properties.style = pre.properties.style;
      }
      return code;
    }
    if (grid) {
      if (code.properties.style) {
        code.properties.style += "display: grid;";
      } else {
        code.properties.style = "display: grid;";
      }
    }
    if (Object.hasOwn(code.properties, "data-line-numbers")) {
      code.properties["data-line-numbers-max-digits"] = lineNumbersMaxDigits.toString().length;
    }
    const fragments = [];
    if (title) {
      const elementContent = {
        type: "element",
        tagName: caption ? "div" : "figcaption",
        properties: {
          "data-rehype-pretty-code-title": "",
          "data-language": lang,
          "data-theme": themeNamesString
        },
        children: [{ type: "text", value: title }]
      };
      onVisitTitle?.(elementContent);
      fragments.push(elementContent);
    }
    fragments.push(pre);
    if (caption) {
      const elementContent = {
        type: "element",
        tagName: "figcaption",
        properties: {
          "data-rehype-pretty-code-caption": "",
          "data-language": lang,
          "data-theme": themeNamesString
        },
        children: [{ type: "text", value: caption }]
      };
      onVisitCaption?.(elementContent);
      fragments.push(elementContent);
    }
    return fragments;
  });
}
var globalHighlighterCache = /* @__PURE__ */ new Map();
var hastParser = unified().use(rehypeParse, { fragment: true });
var src_default = rehypePrettyCode;
function rehypePrettyCode(options = {}) {
  const {
    grid = true,
    theme = "github-dark-dimmed",
    keepBackground = true,
    bypassInlineCode = false,
    defaultLang = "",
    tokensMap = {},
    filterMetaString = (v) => v,
    getHighlighter = getSingletonHighlighter,
    transformers,
    onVisitLine,
    onVisitHighlightedLine,
    onVisitHighlightedChars,
    onVisitTitle,
    onVisitCaption
  } = options;
  const key = JSON.stringify(theme);
  let cachedHighlighter = globalHighlighterCache.get(key);
  if (!cachedHighlighter) {
    cachedHighlighter = getHighlighter({
      themes: isJSONTheme(theme) || typeof theme === "string" ? [theme] : Object.values(theme),
      langs: ["plaintext"]
    });
    globalHighlighterCache.set(key, cachedHighlighter);
  }
  const defaultCodeBlockLang = typeof defaultLang === "string" ? defaultLang : defaultLang.block || "";
  const defaultInlineCodeLang = typeof defaultLang === "string" ? defaultLang : defaultLang.inline || "";
  function getOptions(lang, meta) {
    const multipleThemes = !isJSONTheme(theme) && typeof theme === "object" ? theme : null;
    const singleTheme = isJSONTheme(theme) || typeof theme === "string" ? theme : null;
    return {
      lang,
      meta: { __raw: meta },
      transformers,
      defaultColor: typeof theme === "string" ? theme : false,
      ...multipleThemes ? { themes: multipleThemes } : { theme: singleTheme }
    };
  }
  return async (tree) => {
    const langsToLoad = /* @__PURE__ */ new Set();
    const highlighter = await cachedHighlighter;
    if (!highlighter) return;
    visit(tree, "element", (element, _, parent) => {
      if (isInlineCode(element, parent, bypassInlineCode)) {
        const textElement = element.children[0];
        if (!isText(textElement)) return;
        const value = textElement.value;
        if (!value) return;
        const lang = getInlineCodeLang(value, defaultInlineCodeLang);
        if (lang && lang[0] !== ".") {
          langsToLoad.add(lang);
        }
      }
      if (isBlockCode(element)) {
        const codeElement = element.children[0];
        if (!isElement(codeElement)) return;
        const { lang } = parseBlockMetaString(
          codeElement,
          filterMetaString,
          defaultCodeBlockLang
        );
        if (lang) {
          langsToLoad.add(lang);
        }
      }
    });
    try {
      await Promise.allSettled(
        Array.from(langsToLoad).map((lang) => {
          try {
            return highlighter.loadLanguage(
              lang
            );
          } catch (e) {
            return Promise.reject(e);
          }
        })
      );
    } catch (e) {
      console.error(e);
    }
    visit(tree, "element", (element, _, parent) => {
      if (isInlineCode(element, parent, bypassInlineCode)) {
        const textElement = element.children[0];
        if (!isText(textElement)) return;
        const value = textElement.value;
        if (!value) return;
        const keepLangPart = /\\{:[a-zA-Z.-]+}$/.test(value);
        const strippedValue = keepLangPart ? value.replace(/\\({:[a-zA-Z.-]+})$/, "$1") : value.replace(/{:[a-zA-Z.-]+}$/, "");
        textElement.value = strippedValue;
        const lang = keepLangPart ? "" : getInlineCodeLang(value, defaultInlineCodeLang);
        const isLang = lang[0] !== ".";
        if (!lang) return;
        let codeTree;
        if (isLang) {
          try {
            codeTree = hastParser.parse(
              highlighter.codeToHtml(strippedValue, getOptions(lang))
            );
          } catch {
            codeTree = hastParser.parse(
              highlighter.codeToHtml(strippedValue, getOptions("plaintext"))
            );
          }
        } else {
          const themeNames = getThemeNames(theme);
          const isMultiTheme = typeof theme === "object" && !isJSONTheme(theme);
          const themeKeys = isMultiTheme ? Object.keys(theme) : null;
          const colorsByTheme = themeNames.map(
            (name) => name ? highlighter.getTheme(name).settings.find(
              ({ scope }) => scope?.includes(tokensMap[lang.slice(1)] ?? lang.slice(1))
            )?.settings.foreground ?? "inherit" : "inherit"
          );
          if (isMultiTheme && themeKeys) {
            codeTree = hastParser.parse(
              `<pre><code><span style="${themeKeys.map((key2, i) => `--shiki-${key2}:${colorsByTheme[i]}`).join(";")}">${strippedValue}</span></code></pre>`
            );
          } else {
            codeTree = hastParser.parse(
              `<pre><code><span style="color:${colorsByTheme[0]}">${strippedValue}</span></code></pre>`
            );
          }
        }
        visit(codeTree, "element", replaceLineClass);
        apply(element, {
          tree: codeTree,
          lang: isLang ? lang : ".token",
          inline: true,
          keepBackground,
          theme
        });
      }
      if (isBlockCode(element)) {
        const codeElement = element.children[0];
        if (!isElement(codeElement)) return;
        const textElement = codeElement.children[0];
        const { title, caption, meta, lang } = parseBlockMetaString(
          codeElement,
          filterMetaString,
          defaultCodeBlockLang
        );
        if (!lang || lang === "math") return;
        const lineNumbers = [];
        if (meta) {
          const matches = meta.matchAll(/\B\{(.*?)\}\B/g);
          for (const match of matches) {
            if (match[1]) {
              lineNumbers.push(...rangeParser2(match[1]));
            }
          }
        }
        let lineNumbersMaxDigits = 0;
        const lineIdMap = /* @__PURE__ */ new Map();
        const charsList = [];
        const charsListNumbers = [];
        const charsListIdMap = /* @__PURE__ */ new Map();
        const charsMatches = meta ? [
          ...meta.matchAll(
            /(?<delimiter>["/])(?<chars>.*?)\k<delimiter>(?<charsIdAndOrRange>\S*)/g
          )
        ] : void 0;
        lineNumbers.forEach((lineNumber) => {
          const id = getLineId(lineNumber, meta);
          id && lineIdMap.set(lineNumber, id);
        });
        if (Array.isArray(charsMatches)) {
          charsMatches.forEach((name) => {
            const { chars, charsIdAndOrRange } = name.groups;
            charsList.push(chars);
            if (charsIdAndOrRange === "") {
              charsListNumbers.push([]);
            } else {
              const [range, id] = charsIdAndOrRange.split("#");
              charsListNumbers.push(range ? rangeParser2(range) : []);
              id && charsListIdMap.set(chars, id);
            }
          });
        }
        if (!isText(textElement)) return;
        const strippedValue = textElement.value.replace(/\n$/, "");
        let codeTree;
        try {
          codeTree = hastParser.parse(
            highlighter.codeToHtml(strippedValue, getOptions(lang, meta))
          );
        } catch {
          codeTree = hastParser.parse(
            highlighter.codeToHtml(
              strippedValue,
              getOptions("plaintext", meta)
            )
          );
        }
        let lineCounter = 0;
        const charsHighlighterOptions = {
          ranges: charsListNumbers,
          idsMap: charsListIdMap,
          counterMap: /* @__PURE__ */ new Map()
        };
        const showLineNumbers = getShowLineNumbers(
          meta,
          (charsMatches ?? []).map((match) => {
            const start = match.index ?? 0;
            return [start, start + match[0].length];
          })
        );
        visit(codeTree, "element", (element2) => {
          if (element2.tagName === "code" && showLineNumbers) {
            if (element2.properties) {
              element2.properties["data-line-numbers"] = "";
            }
            if (showLineNumbers.startAt !== null) {
              const startAt = showLineNumbers.startAt - 1;
              lineNumbersMaxDigits = startAt;
              if (element2.properties) {
                element2.properties.style = `counter-set: line ${startAt};`;
              }
            }
          }
          if (Array.isArray(element2.properties?.className) && element2.properties?.className?.[0] === "line") {
            if (grid && toString(element2) === "") {
              element2.children = [{ type: "text", value: " " }];
            }
            replaceLineClass(element2);
            onVisitLine?.(element2);
            lineCounter++;
            if (lineNumbers.includes(lineCounter)) {
              element2.properties["data-highlighted-line"] = "";
              const lineId = lineIdMap.get(lineCounter);
              if (lineId) {
                element2.properties["data-highlighted-line-id"] = lineId;
              }
              onVisitHighlightedLine?.(element2, lineId);
            }
            charsHighlighter(
              element2,
              charsList,
              charsHighlighterOptions,
              onVisitHighlightedChars
            );
            lineNumbersMaxDigits++;
          }
        });
        apply(element, {
          tree: codeTree,
          lang,
          title,
          caption,
          keepBackground,
          grid,
          lineNumbersMaxDigits,
          theme,
          onVisitTitle,
          onVisitCaption
        });
      }
    });
  };
}

export { src_default as default, rehypePrettyCode };
